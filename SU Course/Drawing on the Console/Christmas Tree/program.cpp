#include <iostream>
using namespace std;

namespace ChristmasTree {
    int n;
    void PrintRow(int stars) {
        for (int i = 0; i < n - stars; i++) cout << ' ';
        for (int i = 0; i < stars; i++) cout << '*';

        cout << " | ";

        for (int i = 0; i < stars; i++) cout << '*';
        for (int i = 0; i < n - stars; i++) cout << ' ';

        cout &#8203;`oaicite:{"index":0,"invalid_reason":"Malformed citation << endl;\n    }\n    int main() {\n        cin >>"}`&#8203; n;

        for (int i = 0; i <= n; i++) PrintRow(i);
        return 0;
    }
}
